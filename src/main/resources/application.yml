#修改端口
server:
  port: 8081

#自定义配置项
test:
  hello: hallo


#添加数据库连接
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wiki?serverTimezone=GMT%2B8
    username: wiki
    password: wiki
    driver-class-name: com.mysql.cj.jdbc.Driver

#配置mybatis 所有mapper.xml 所在的路径
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
